package pt.romain.photosback;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class PhotosBackApplicationTests
{

    @Test
    void contextLoads()
    {
        // Le corps de la méthode est vide car l'objectif est simplement
        // de vérifier que le contexte Spring Boot se charge sans erreur.
        // Si le contexte se charge, le test passe. Sinon, il échoue.
    }

}
